cmake_minimum_required(VERSION 3.22)

project(MidiGridAnalyzer VERSION 1.0.0 LANGUAGES C CXX)

set(CMAKE_CXX_STANDARD 20)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)

# Fetch JUCE 7 framework
include(FetchContent)
FetchContent_Declare(
    juce
    GIT_REPOSITORY https://github.com/juce-framework/JUCE.git
    GIT_TAG        7.0.12
)
FetchContent_MakeAvailable(juce)

# Embed WAV sound assets
juce_add_binary_data(MidiGridAnalyzerAssets
    HEADER_NAME "BinaryData.h"
    NAMESPACE "BinaryData"
    SOURCES
        assets/sounds/wood_clave.wav
        assets/sounds/Drum_Stick_Click_01.wav
)

# Define JUCE Audio Plugin & Standalone target
juce_add_plugin(MidiGridAnalyzer
    COMPANY_NAME "Gridlock"
    IS_SYNTH FALSE
    NEEDS_MIDI_INPUT TRUE
    NEEDS_MIDI_OUTPUT TRUE
    IS_MIDI_EFFECT FALSE
    EDITOR_WANTS_KEYBOARD_FOCUS TRUE
    COPY_PLUGIN_AFTER_BUILD FALSE
    PLUGIN_MANUFACTURER_CODE Grdl
    PLUGIN_CODE Mga1
    FORMATS VST3 AU Standalone
    PRODUCT_NAME "MIDI Grid Analyzer"
)

# Source files
target_sources(MidiGridAnalyzer PRIVATE
    plugin/HitEvent.h
    plugin/RingBuffer.h
    plugin/ClickGenerator.h
    plugin/ClickGenerator.cpp
    plugin/RemoteControlServer.h
    plugin/RemoteControlServer.cpp
    plugin/PluginProcessor.h
    plugin/PluginProcessor.cpp
    plugin/GridComponent.h
    plugin/GridComponent.cpp
    plugin/PluginEditor.h
    plugin/PluginEditor.cpp
)

# Link JUCE Modules and Embedded Binary Data
target_link_libraries(MidiGridAnalyzer PRIVATE
    MidiGridAnalyzerAssets
    juce::juce_audio_utils
    juce::juce_audio_processors
    juce::juce_audio_basics
    juce::juce_gui_basics
    juce::juce_graphics
    juce::juce_opengl
    juce::juce_audio_formats
)

target_compile_definitions(MidiGridAnalyzer PRIVATE
    JUCE_WEB_BROWSER=0
    JUCE_USE_CURL=0
    JUCE_VST3_CAN_REPLACE_VST2=0
)

# ── Standalone C++ Networking Integration Test Executable ─────────
juce_add_console_app(RemoteControlServerTests
    PRODUCT_NAME "Remote Control Server Tests"
)

target_sources(RemoteControlServerTests PRIVATE
    plugin/RemoteControlServer.h
    plugin/RemoteControlServer.cpp
    plugin/RemoteControlServerTests.cpp
)

target_link_libraries(RemoteControlServerTests PRIVATE
    juce::juce_audio_processors
    juce::juce_audio_basics
    juce::juce_gui_basics
    juce::juce_core
)

target_compile_definitions(RemoteControlServerTests PRIVATE
    JUCE_WEB_BROWSER=0
    JUCE_USE_CURL=0
    JUCE_MODAL_LOOPS_PERMITTED=1
)

# ── P1 Logic Unit Tests ─────────
juce_add_console_app(LogicTests
    PRODUCT_NAME "Logic Tests"
)

target_sources(LogicTests PRIVATE
    plugin/Crypto.h
    plugin/DrumMap.h
    plugin/GridComponent.h
    plugin/GridComponent.cpp
    plugin/HitEvent.h
    plugin/RingBuffer.h
    plugin/Timing.h
    plugin/Theme.h
    plugin/ClickGenerator.h
    plugin/ClickGenerator.cpp
    plugin/PluginProcessor.h
    plugin/PluginProcessor.cpp
    plugin/PluginEditor.h
    plugin/PluginEditor.cpp
    plugin/RemoteControlServer.h
    plugin/RemoteControlServer.cpp
    plugin/LogicTests.cpp
)

target_link_libraries(LogicTests PRIVATE
    MidiGridAnalyzerAssets
    juce::juce_audio_processors
    juce::juce_audio_basics
    juce::juce_gui_basics
    juce::juce_graphics
    juce::juce_core
    juce::juce_audio_formats
    juce::juce_opengl
)

target_compile_definitions(LogicTests PRIVATE
    JUCE_WEB_BROWSER=0
    JUCE_USE_CURL=0
    JUCE_MODAL_LOOPS_PERMITTED=1
    JucePlugin_Name="LogicTests"
    JucePlugin_Desc="LogicTests"
    JucePlugin_Manufacturer="Gridlock"
    JucePlugin_VersionString="1.0.0"
)
